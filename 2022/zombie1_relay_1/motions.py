#!/usr/bin/python3

import time

"""
    Motions that can be run on each of the actuators connected by the relay control hats

    Each relay class has methods for:
        extension: extend()
        contraction: contract()

    function sample(relay1, relay2, relay3):
        relay1.contract() --or--
        relay1.extend()
    """